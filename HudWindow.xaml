<Window x:Class="ETSOverlay.HudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HudWindow"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="True"
        WindowStartupLocation="Manual">

    <Window.Resources>
        <Style x:Key="HudBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource HeaderButtonBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource HeaderIconBrush}"/>
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HeaderButtonHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseBtnStyle" TargetType="Button" BasedOn="{StaticResource HudBtnStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainBorder" MouseEnter="MainBorder_MouseEnter" MouseLeave="MainBorder_MouseLeave">
        <Grid.LayoutTransform>
            <ScaleTransform x:Name="UIScaleTransform" ScaleX="1.15" ScaleY="1.15"/>
        </Grid.LayoutTransform>

        <StackPanel Orientation="Horizontal" Margin="16">
            <!-- Main Data Container -->
            <Grid x:Name="DataContainer" SizeChanged="DataContainer_SizeChanged">
                <Border x:Name="DataBg" Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="8">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="14" Opacity="{DynamicResource ShadowOpacity}" Direction="270" ShadowDepth="3"/>
                    </Border.Effect>
                </Border>
                
                <Border MinHeight="38" Padding="12,0">
                    <StackPanel Orientation="Horizontal">

                    <!-- TB STATUS -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse x:Name="TbStatusDot" Width="8" Height="8" Fill="{DynamicResource AccentBrush_Sim}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TbStatus" Text="TB • ONLINE" Foreground="{DynamicResource AccentBrush_Sim}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Border Width="1" Background="{DynamicResource CardBorderBrush}" Margin="12,4" HorizontalAlignment="Center"/>

                    <!-- DISTANCE -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Path Data="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z" Fill="{DynamicResource AccentBrush_Distance}" Stretch="Uniform" Width="12" Height="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="DistanceInfo" Text="0 / 0 km" Foreground="{DynamicResource AccentBrush_Distance}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Grid x:Name="SpeedSeparator" Tag="25">
                        <Border Width="1" Background="{DynamicResource CardBorderBrush}" Margin="0,4" HorizontalAlignment="Center"/>
                    </Grid>

                    <!-- SPEED -->
                    <StackPanel x:Name="SpeedContainer" Orientation="Horizontal" VerticalAlignment="Center" ClipToBounds="True" RenderTransformOrigin="0.5,0.5">
                        <StackPanel.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </StackPanel.RenderTransform>
                        <TextBlock x:Name="SpeedLabel" Text="SPEED: " Foreground="{DynamicResource MainTextBrush}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center" Margin="0,2,2,0"/>
                        <TextBlock x:Name="SpeedValue" Text="0" Foreground="{DynamicResource AccentBrush_Speed}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="SpeedUnit" Text="km/h" Foreground="{DynamicResource MutedTextBrush}" FontSize="10" VerticalAlignment="Bottom" Margin="2,0,0,1"/>
                    </StackPanel>

                    <Grid x:Name="MaxSpeedSeparator" Tag="25">
                        <Border Width="1" Background="{DynamicResource CardBorderBrush}" Margin="0,4" HorizontalAlignment="Center"/>
                    </Grid>

                    <!-- MAX SPEED -->
                    <StackPanel x:Name="MaxSpeedContainer" Orientation="Horizontal" VerticalAlignment="Center" ClipToBounds="True" RenderTransformOrigin="0.5,0.5">
                        <StackPanel.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </StackPanel.RenderTransform>
                        <TextBlock x:Name="MaxSpeedLabel" Text="MAX: " Foreground="{DynamicResource MainTextBrush}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center" Margin="0,2,2,0"/>
                        <TextBlock x:Name="MaxSpeedValue" Text="0" Foreground="{DynamicResource AccentBrush_Max}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="MaxUnit" Text="km/h" Foreground="{DynamicResource MutedTextBrush}" FontSize="10" VerticalAlignment="Bottom" Margin="2,0,0,1"/>
                    </StackPanel>

                    <Grid x:Name="TypeSeparator" Tag="25">
                        <Border Width="1" Background="{DynamicResource CardBorderBrush}" Margin="0,4" HorizontalAlignment="Center"/>
                    </Grid>

                    <!-- TYPE -->
                    <StackPanel x:Name="TypeContainer" Orientation="Horizontal" VerticalAlignment="Center" ClipToBounds="True" RenderTransformOrigin="0.5,0.5">
                        <StackPanel.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </StackPanel.RenderTransform>
                        <TextBlock x:Name="DeliveryType" Text="N/A" Foreground="{DynamicResource AccentBrush_Type}" FontSize="14" FontWeight="Heavy" VerticalAlignment="Center"/>
                    </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- HOVER BUTTONS BORDER -->
            <Border x:Name="HoverActionPanel" Background="{DynamicResource HeaderBackgroundBrush}" CornerRadius="8" Padding="6" Margin="10,0,0,0" Opacity="0" Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="14" Opacity="0.5" Direction="270" ShadowDepth="3"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Click="BtnSettings_Click" Style="{StaticResource HudBtnStyle}" ToolTip="Settings" Margin="0,0,4,0">
                        <Path Data="M19.4 13c0-.33.05-.66.05-1s-.02-.67-.05-1l2.1-1.66c.2-.15.25-.42.13-.64l-2-3.46c-.12-.22-.4-.3-.6-.22l-2.5 1c-.5-.4-1.07-.73-1.68-.98l-.4-2.65C14.46 2.18 14.25 2 14 2h-4c-.25 0-.46.18-.5.42l-.37 2.65c-.6.25-1.17.58-1.68.98l-2.5-1c-.22-.08-.48 0-.6.22l-2 3.46c-.13.22-.07.49-.12.64L4.57 11c-.03.33-.07.67-.07 1s.04.67.07 1l-2.1 1.66c-.2.15-.25.42-.12.64l2 3.46c.12.22.38.3.6.22l2.5-1c.5.4 1.07.73 1.68.98l.38 2.65c.03.24.24.42.49.42h4c.25 0 .46-.18.5-.42l.37-2.65c.6-.25 1.17-.58 1.68-.98l2.5 1c.22.08.48 0 .6-.22l2-3.46c.12-.22.07-.49-.12-.64L19.4 13zM12 15.5c-1.93 0-3.5-1.57-3.5-3.5s1.57-3.5 3.5-3.5 3.5 1.57 3.5 3.5-1.57 3.5-3.5 3.5z" Fill="{DynamicResource HeaderIconBrush}" Stretch="Uniform" Width="12" Height="12"/>
                    </Button>
                    <Button x:Name="BtnTopmost" Click="BtnTopmost_Click" Style="{StaticResource HudBtnStyle}" ToolTip="Pin" Margin="0,0,4,0">
                        <Path x:Name="PinIcon" Data="M16,12V4H17V2H7V4H8V12L6,14V16H11V22H13V16H18V14L16,12Z" Fill="{DynamicResource HeaderIconBrush}" Stretch="Uniform" Width="10" Height="10"/>
                    </Button>
                    <Button Click="BtnMinimize_Click" Style="{StaticResource HudBtnStyle}" Margin="0,0,4,0">
                        <Path Data="M0,0 L12,0" Stroke="{DynamicResource HeaderIconBrush}" StrokeThickness="2" Stretch="Uniform" Width="10" VerticalAlignment="Center"/>
                    </Button>
                    <Button Click="BtnClose_Click" Style="{StaticResource CloseBtnStyle}">
                        <Path Data="M0,0 L10,10 M0,10 L10,0" Stroke="{DynamicResource HeaderIconBrush}" StrokeThickness="2" Stretch="Uniform" Width="8" Height="8" VerticalAlignment="Center"/>
                    </Button>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>
