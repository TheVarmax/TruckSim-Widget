<Window x:Class="ETSOverlay.CustomColorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customize Colors" Width="380" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True" ShowInTaskbar="False">
    <Window.Resources>
        <!-- ScrollViewer Style for list -->
        <Style TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="MaxHeight" Value="500"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MainTextBrush}"/>
        </Style>

        <!-- General Button Style -->
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource MainTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="12" Padding="20" Margin="15">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="{DynamicResource ShadowOpacity}" Direction="270" ShadowDepth="4"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock x:Name="TitleBlock" Text="Customize Tile Colors" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource MainTextBrush}"/>

            <ScrollViewer Grid.Row="1" Margin="0,0,0,20">
                <ItemsControl x:Name="ColorItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Title -->
                                <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" Foreground="{DynamicResource MainTextBrush}"/>

                                <!-- Color Button -->
                                <Button Grid.Column="1" Click="Palette_Click" Style="{StaticResource DialogButton}" Background="{DynamicResource InputBackgroundBrush}" Height="36" Cursor="Hand">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Width="20" Height="20" CornerRadius="4" Background="{Binding CurrentColorBrush}" Margin="0,0,10,0" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1"/>
                                        <TextBlock Text="{Binding HexValue}" Foreground="{DynamicResource MainTextBrush}" FontSize="14" FontFamily="Consolas" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Buttons -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="BtnCancel" Grid.Column="0" Content="Cancel" Click="BtnCancel_Click" Background="{DynamicResource InputBackgroundBrush}" Style="{StaticResource DialogButton}"/>
                <Button x:Name="BtnSave" Grid.Column="2" Content="Save" Click="BtnSave_Click" Background="#43A047" Style="{StaticResource DialogButton}"/>
            </Grid>
        </Grid>
    </Border>
</Window>
